# Raw dataset — too large for git / 50 MB submission limit (provided by HackerEarth).
# Precomputed artifacts in parksight/data/processed/ ARE committed so the app runs instantly.
jan to may police violation_anonymized791b166.csv
Astram event data_anonymized*.csv
*.csv

# Python
__pycache__/
*.pyc
.venv/
venv/
.env

# Streamlit
.streamlit/secrets.toml

# OS / IDE
.DS_Store
Thumbs.db

# Keep processed artifacts + assets (needed for the demo)
!parksight/data/processed/*.parquet
!parksight/data/processed/*.json
